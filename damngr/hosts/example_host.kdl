host {
    example_host {
        modules {
            example_module
            another_example_module
        }
        services {
            example_root_service // implicit root=#true
        }
    }
}
